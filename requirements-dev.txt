# Development tooling (lint/format/typecheck/tests/bench/docs).
#
# Keep this file relatively lightweight (no large runtime/framework deps like
# torch/neuromancer/physicsnemo/torchphysics). Those belong in requirements-extra.txt.
#
# Versions are bounded for reproducible CI while still allowing minor updates.

# --- Lint / format / types -----------------------------------------------------
ruff==0.7.1                     # pinned to keep formatting/lint output stable in CI
pre-commit>=4.0.1,<5.0
codespell>=2.3.0,<3.0
mypy>=1.7.0,<2.0
types-PyYAML>=6.0.12.20250915,<7.0
tomli>=2.0.1,<3.0; python_version<'3.11'   # for tools reading pyproject.toml on Py<3.11

# --- Testing -------------------------------------------------------------------
pytest>=8.4,<9.0
pytest-cov>=6.2,<8.0
coverage>=7.10.6,<8.0            # explicit for consistent coverage reports (pytest-cov depends on coverage)
pytest-xdist>=3.7,<5.0
pytest-randomly>=3.15,<5.0
pytest-timeout>=2.4,<3.0
pytest-benchmark>=5.2.3,<6.0

# --- Benchmarks / experiment metadata ------------------------------------------
py-cpuinfo>=9.0.0,<10.0
psutil>=7.1.3,<8.0

# --- Docs / report helpers -----------------------------------------------------
mkdocs>=1.5,<2.0
mkdocs-material>=9.5,<10.0
mkdocs-mermaid2-plugin>=1.2.0,<2.0
pymdown-extensions>=10.0,<11.0
